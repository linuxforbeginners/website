<h1>Articles</h1>

The website has <%= Article.public_count %> article<% unless Article.public_count == 1; %>s<% end %>!

<div class="article-list">
  <% @articles.each do |article| %>
    <div class="article">
      <h3 class="title"><%= link_to article.title, article %></h3>
      <div class="desc"><%= article.body.to_s[...50] %><% if article.body.length > 50; %>...<% end %></div>
      <div class="visitit">
        <%= link_to "Read", article, class: "button" %>
      </div>
    </div>
  <% end %>
</div>

<%= link_to "New Article", new_article_path %>
